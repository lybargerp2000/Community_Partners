@model IEnumerable<CommunityPartners.Models.RequestServicePartners>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PartnerId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProposalDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Accepted)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RatingHelpfulnessId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PayPalId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PartnerId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProposalDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Accepted)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RatingHelpfulnessId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PayPalId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RequestServicePartnersId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RequestServicePartnersId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RequestServicePartnersId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
